import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { Providers } from '@/components/providers'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Business Phone System - Cloud Communication Platform',
  description: 'Comprehensive cloud-based business phone system with VoIP capabilities, call management, team collaboration, and analytics for modern workforces.',
  keywords: 'VoIP, business phone, cloud communication, WebRTC, team collaboration',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Providers>
          {children}
        </Providers>
      </body>
    </html>
  )
}