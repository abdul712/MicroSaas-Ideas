import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { Providers } from '@/components/providers'
import { Toaster } from '@/components/ui/toaster'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Inventory Management - Real-time Stock Control',
  description: 'Comprehensive inventory management platform with real-time tracking, automated reordering, and multi-location warehouse management.',
  keywords: ['inventory management', 'stock control', 'warehouse management', 'barcode scanning'],
  authors: [{ name: 'Inventory Management Team' }],
  viewport: 'width=device-width, initial-scale=1',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Providers>
          {children}
          <Toaster />
        </Providers>
      </body>
    </html>
  )
}