import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import { Providers } from '@/components/providers';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Business Process Documenter - AI-Powered Workflow Management',
  description: 'Create, manage, and optimize your business processes with visual process mapping, step-by-step guides, and compliance tracking.',
  keywords: 'business process, workflow, documentation, compliance, SOP, standard operating procedures',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <Providers>
          {children}
        </Providers>
      </body>
    </html>
  );
}