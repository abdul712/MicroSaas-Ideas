import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { Providers } from '@/components/providers'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Business Trend Analyzer - AI-Powered Market Intelligence',
  description: 'Discover patterns, predict trends, and make data-driven decisions with our AI-powered business intelligence platform.',
  keywords: 'business intelligence, trend analysis, market intelligence, AI predictions, business analytics',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Providers>
          {children}
        </Providers>
      </body>
    </html>
  )
}